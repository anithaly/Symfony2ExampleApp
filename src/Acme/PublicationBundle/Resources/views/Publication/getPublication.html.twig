{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Publication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ entity.title }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ entity.category.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ entity.content }}</td>
            </tr>
            <tr>
                <th>Tags</th>
                <td>
                    {% for tag in entity.tags %}
                        <p>{{ tag.name }}</p>
                    {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-default" href="{{ path('publications_edit_publication', { 'id': entity.id }) }}">
        Edit
    </a>
    {{ form(delete_form) }}

{% include "AcmePublicationBundle:Comment:_new.html.twig" with {'form':comment_form, 'publication':entity} %}

{% include "AcmePublicationBundle:Comment:_index.html.twig" with {'entities':entity.comments} %}

{% endblock %}
