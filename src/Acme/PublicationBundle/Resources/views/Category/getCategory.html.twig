{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-default" href="{{ path('categories_edit_category', { 'id': entity.id }) }}">
        Edit
    </a>
    {{ form(delete_form) }}
{% endblock %}
